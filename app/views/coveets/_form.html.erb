<%= form_with(model: coveet) do |form| %>
  <% if coveet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coveet.errors.count, "error") %> prohibited this coveet from being saved:</h2>

      <ul>
        <% coveet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cuerpo %>
    <%= form.text_area :cuerpo %>
  </div>

  <div class="field">
    <%= form.label :likes %>
    <%= form.number_field :likes %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
